# vite 7 is newer than the peer range @builder.io/vite-plugin-jsx-loc declares (^4 || ^5).
# The plugin works, so accept the mismatch here rather than at every install site — without this
# `npm ci` fails outright, which is what breaks the Render build.
legacy-peer-deps=true
