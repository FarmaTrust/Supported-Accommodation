import{describe,expect,it}from"vitest";
import{ageOnDate,assertYoungPersonAge,dateRangeIssue,youngPersonAgeIssue}from"./inputValidation";
describe("authoritative input validation",()=>{it("rejects end dates before or equal to start with a corrective field message",()=>{expect(dateRangeIssue(200,100,"Shift")).toMatchObject({field:"endsAt",overrideAvailable:false});expect(dateRangeIssue(100,100,"Shift")?.message).toContain("must be after");expect(dateRangeIssue(100,101,"Shift")).toBeNull();});it("calculates age using calendar birthdays",()=>{const dob=Date.UTC(2012,8,20);expect(ageOnDate(dob,Date.UTC(2026,8,19))).toBe(13);expect(ageOnDate(dob,Date.UTC(2026,8,20))).toBe(14);});it("offers only the minimum-age policy as overridable",()=>{expect(youngPersonAgeIssue(Date.UTC(2013,0,1),Date.UTC(2026,0,1))).toMatchObject({field:"dateOfBirth",overrideAvailable:true});expect(youngPersonAgeIssue(Date.UTC(2027,0,1),Date.UTC(2026,0,1))).toMatchObject({overrideAvailable:false});});it("requires manager authority and a meaningful reason for an under-14 override",()=>{const input={dateOfBirth:Date.UTC(2013,0,1),onDate:Date.UTC(2026,0,1)};expect(()=>assertYoungPersonAge({...input,canOverride:false,overrideReason:"Documented exceptional placement need"})).toThrow(/Only an owner or registered manager/);expect(()=>assertYoungPersonAge({...input,canOverride:true,overrideReason:"short"})).toThrow(/at least 20/);expect(assertYoungPersonAge({...input,canOverride:true,overrideReason:"Exceptional placement authorised after safeguarding review."}).overridden).toBe(true);});});
