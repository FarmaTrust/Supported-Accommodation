import{describe,expect,it}from"vitest";
import{independentReviewAllowed,missingReviewAudiences,qualityPeriodError,REG32_MAX_PERIOD_MS,workedMinutes}from"./upgradeRules";
describe("recovered upgrade governance rules",()=>{it("enforces positive Regulation 32 periods no longer than six months",()=>{expect(qualityPeriodError(100,100)).toMatch(/after/);expect(qualityPeriodError(0,REG32_MAX_PERIOD_MS+1)).toMatch(/six months/);expect(qualityPeriodError(0,REG32_MAX_PERIOD_MS)).toBeNull();});it("requires disposition evidence for all four consultation audiences",()=>{const rows=[{audience:"young_person",responseStatus:"responded"},{audience:"placing_authority",responseStatus:"no_response"},{audience:"staff",responseStatus:"declined"},{audience:"professional",responseStatus:"invited"}];expect(missingReviewAudiences(rows)).toEqual(["professional"]);rows[3].responseStatus="not_applicable";expect(missingReviewAudiences(rows)).toEqual([]);});it("blocks self-approval against any recorded author or completer",()=>{expect(independentReviewAllowed(7,[2,3])).toBe(true);expect(independentReviewAllowed(7,[2,7])).toBe(false);});it("calculates non-negative worked minutes",()=>{expect(workedMinutes(0,3_600_000)).toBe(60);expect(workedMinutes(5_000,1_000)).toBe(0);});});
